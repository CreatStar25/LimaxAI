---
// 语言首页重定向至 /{lang}/blog/
import { getAllLocales } from '../../lib/pages';

export async function getStaticPaths() {
  return getAllLocales().map((lang) => ({
    params: { lang },
  }));
}

const { lang } = Astro.params;
return Astro.redirect(`/${lang}/blog/`, 301);
---
