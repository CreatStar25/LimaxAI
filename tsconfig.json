{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "strictNullChecks": true,
    "baseUrl": ".",                // 确保有这行
    "paths": {
      "@assets/*": ["src/assets/*"] // 添加这一行
    }
  }
}